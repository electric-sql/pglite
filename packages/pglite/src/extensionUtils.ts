
export async function loadExtensionBundle(mod: any, bundlePath: URL) {
  // Async load the extension bundle tar file
  // ** could be a URL or a file path depending on the environment
  // extract the tar
  // load in the VFS
}
