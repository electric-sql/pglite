--- postgresql-16.2/src/include/common/file_utils.h	2024-02-05 22:41:37.000000000 +0100
+++ postgresql-16.2-wasm/src/include/common/file_utils.h	2024-05-06 08:20:15.251406348 +0200
@@ -27,10 +27,14 @@
 struct iovec;					/* avoid including port/pg_iovec.h here */
 
 #ifdef FRONTEND
+#if !defined(fsync_fname)
 extern int	fsync_fname(const char *fname, bool isdir);
+#endif
 extern void fsync_pgdata(const char *pg_data, int serverVersion);
 extern void fsync_dir_recurse(const char *dir);
+#if !defined(durable_rename)
 extern int	durable_rename(const char *oldfile, const char *newfile);
+#endif
 extern int	fsync_parent_path(const char *fname);
 #endif
 
